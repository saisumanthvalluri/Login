import { Link } from "react-router-dom";

const SignInPage = () => {
    return (
        <div>
            <h1>Sign In</h1>
            <Link to="/sign-up">Sign Up</Link>
        </div>
    );
};

export default SignInPage;
